void main() {
  late final String name;
  // do something, go to api
  name = 'thomas';
  print(name);
  // late -> 초기값 없이 변수 선언 가능하게 해주는 키워드
  // final = ONLY 1 ASSIGNMENT
  // 그런데 late 덕분에 초기값 설정 없이도 통과되었다!
}
